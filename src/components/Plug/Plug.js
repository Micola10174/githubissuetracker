import React from 'react';
import './Plug.scss';

const Plug = ({text}) => {
    return (
        <div className="wrapper-plug">
            <h2>{text}</h2>
        </div>
    )
}

export default Plug
