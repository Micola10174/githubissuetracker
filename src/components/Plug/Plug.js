import React from 'react';
import './Plug.css';

const Plug = ({text}) => {
    return (
        <div className="wrapper-plug">
            <h2>{text}</h2>
        </div>
    )
}

export default Plug
